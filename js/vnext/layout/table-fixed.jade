-
  var tableStyle = {
    'border-collapse': 'collapse',
    'margin': 0,
    'table-layout': 'fixed',
  };

.table-container
  .fixed-header
    table.table.table-bordered(style=tableStyle)
      colgroup.column-group
      thead.header
  .viewport
    table.table.table-bordered(style=tableStyle)
      colgroup.column-group
      tbody
        tr.top-filler
        tr.bottom-filler
      tfoot.footer
